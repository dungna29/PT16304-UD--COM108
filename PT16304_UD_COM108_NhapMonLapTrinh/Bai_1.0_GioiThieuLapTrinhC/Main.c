#include <stdio.h>
// ĐỪNG CHÉP CODE

/*
 *	Các phím tắt trong Visual Studio
 *	1. F5 - Để chạy chương trình
 *	2. Ctrl + K + D dùng để Format code trên toàn bộ 1 file C
 *	3. Ctrl + K + C Comment code tại dòng của con trỏ
 *	4. Ctrl + K + U Mở Comment code tại dòng của con trỏ
 *	5. Ctrl + D thực hiện nhân bản dòng code đó lên 1 lần
 *	BẮT BUỘC PHẢI SỬ DỤNG NHIỀU KHI CODE TRÊN IDE
 *	CTRL + SPACE (Gọi ra gợi ý code)
 */
 //Trong lập trình C bắt buộc phải khai báo hàm main
int main()//() dấu mở ngoặc ám chỉ đây là hàm
{//Mở lệnh
	printf("Chao cac ban den voi lap Trinh C tai POLY");//Dấu chấm phẩy dùng để kết thúc câu lệnh
		
	//BodyCode
	//Các lập trình viên sẽ thực hiện các câu lệnh ở đây chứ không phải ngoài hàm
}//Đóng lệnh - Nếu không đóng sẽ báo lỗi
